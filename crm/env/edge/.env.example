# Supabase Edge Functions (Deno) environment example
# Copy this file to your secure env location and DO NOT commit real secrets.

# Supabase project details
SUPABASE_URL=https://YOUR-PROJECT.ref.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Webhook signatures
HMAC_SHARED_SECRET_INSTANTLY=replace-with-random-hex-or-strong-secret
HMAC_SHARED_SECRET_CALCOM=replace-with-random-hex-or-strong-secret

# Development helpers
# When true, signature verification is bypassed (ONLY FOR LOCAL DEV!)
DEV_SIGNATURE_BYPASS=true

# Logging
LOG_LEVEL=info
